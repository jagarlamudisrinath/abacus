# Database
DB_PASSWORD=alama123

# JWT Secret (change this in production!)
JWT_SECRET=your-jwt-secret-change-in-production

# Node Environment
NODE_ENV=development
PORT=3001

# Database URL (for local development without Docker)
DATABASE_URL=postgresql://alama:alama123@localhost:5432/alama_abacus

# Initial Users (optional, for docker compose up)
# Format: comma-separated identifier:name pairs
# Identifier can be email (contains @) or student_id
# Example: INIT_USERS=teacher@school.com:Teacher Name,student@school.com:Student Name,STU001:Another Student
INIT_USERS=
