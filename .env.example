# Database
DB_PASSWORD=alama123

# JWT Secret (change this in production!)
JWT_SECRET=your-jwt-secret-change-in-production

# Node Environment
NODE_ENV=development
PORT=3001

# Database URL (for local development without Docker)
DATABASE_URL=postgresql://alama:alama123@localhost:5432/alama_abacus

# Superuser credentials (required for initial setup)
# The superuser is created on first server startup if not exists
SUPERUSER_EMAIL=admin@example.com
SUPERUSER_PASSWORD=change-this-password
SUPERUSER_NAME=Super Admin

# Frontend API URL (optional)
# Defaults to '/api' which works for both local dev (with proxy) and Docker (nginx proxy)
# Only set this if you need to override the default behavior
# REACT_APP_API_URL=/api
